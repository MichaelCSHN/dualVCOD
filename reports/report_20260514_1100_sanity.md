# Repository Sanity Check — 2026-05-14 11:00

Post-reboot check following E-53a archive (commit `f545a4a`).

---

## 1. Git Health

| Check | Result |
|-------|--------|
| `git pull` | Already up to date |
| Branch | main, up to date with origin/main |
| Working tree | Clean (no modified tracked files) |
| HEAD | `f545a4a` — archive e53a multiscale dense probe and reports |

**Untracked files** (safe — all in local_runs/ or new WIP):
- `configs/autoresearch/expl_52_effb0_densefg_30ep.json` (E-52 config, not yet archived)
- `local_runs/autoresearch/` — experiment outputs (JSONs only; no .pth leaked)
- `reports/report_20260513_*.md` — recent reports
- `tools/reeval_2x2_backbone_epochs.py` — new reeval tool

**Tracked under `local_runs/`**: only `metadata.json` / `metrics.json` / `profiles.jsonl` / `config.json` / `trial_config.json` / `safety_check.json` / `final_metrics.json`. No .pth files are tracked. ✓

**reeval artifacts**: `local_runs/reeval_53a.json` is correctly ignored by `.gitignore` line 79 (`local_runs/reeval_*.json`). ✓

---

## 2. .gitignore Coverage

| Pattern | Covered | Line | Notes |
|---------|---------|------|-------|
| `*.pth` | YES | 32 | Global pattern, catches all `.pth` anywhere |
| `local_runs/autoresearch/*/checkpoint*.pth` | YES | 32 | Inherited from `*.pth`; no explicit sub-rule needed |
| Dataset dirs (`data/`, `datasets/`, `MoCA/`, etc.) | YES | 20–28 | 9 dataset patterns |
| `__pycache__/` | YES | 2 | Standard Python |
| `*.log` / `*.tmp` / `tmp/` / `temp/` | YES | 47, 51–53 | All temp artifacts covered |
| `.claude/` | YES | 69 | Claude Code internals |

**Verdict**: All critical patterns covered. No .gitignore gaps.

---

## 3. E-53a Artifact Audit

| File | Exists |
|------|--------|
| `reports/report_20260514_0800_e53a_final.md` | YES |
| `reports/error_analysis/E53a_go_nogo_20260514.md` | YES |
| `configs/autoresearch/expl_53a_effb0_msdense_8ep.json` | YES |

**Go/No-Go outcome**: **FAILED** (2/7 criteria pass). Direction CLOSED — E-53b 30ep will not run.

**Key result**: E-53a pf_mIoU 0.7540 vs E-51 0.8372 (−0.0832). Multi-scale dense supervision degraded all metrics; catastrophic collapse on pygmy_seahorse_0 (0.0079 IoU).

---

## 4. Mainline Default Integrity

| Parameter | Default Value | Check |
|-----------|--------------|-------|
| `use_s4` | `False` (model.py:134) | ✓ — only set True when head_type="dense_fg_aux_ms" (model.py:389) |
| `dense_fg_aux_ms` | Not default — activated only by explicit `head_type="dense_fg_aux_ms"` | ✓ — no auto-activation path |
| `dense_fg_s4_weight` | `0.0` (loss.py:174) | ✓ — only non-zero when head_type="dense_fg_aux_ms" (run_trial_minimal.py:305) |
| `dense_fg_aux` construction | Builds OK when `head_type in ("dense_fg_aux", "dense_fg_aux_ms")` (model.py:396) | ✓ — single head path unchanged |
| EffB0 + dense_fg_aux smoke | E-51 (8ep): train_mIoU 0.3665→0.7513, val_mIoU 0.223→0.3345 | ✓ — converges cleanly, no NaN |

**Verdict**: Mainline (`current_direct_bbox` and `dense_fg_aux` without `_ms`) is clean. E-53a's s4 pathway is fully gated behind `head_type="dense_fg_aux_ms"` and does not leak into the default code path.

---

## 5. Canonical Baseline (for README reference)

| Metric | E-40 (MV3-Small, 30ep) | E-51 (EffB0, 8ep) |
|--------|------------------------|---------------------|
| pf_mIoU | 0.8564 | 0.8372 |
| Training val_mIoU | — | 0.3345 |

E-40 is the canonical MV3-Small + dense_fg_aux baseline at 30ep. E-51 is the EffB0 + dense_fg_aux 8ep probe.

---

## 6. README Status: OUTDATED

| Issue | Detail |
|-------|--------|
| Status line | "Project under clean retraining" — stale; Phase 2 exploration complete (E-01 → E-53a, 50+ trials) |
| No canonical baseline | E-40 (MV3-Small, 30ep, dense_fg_aux, pf_mIoU 0.8564) should be listed |
| No EffB0 variant | E-51 (EffB0, 8ep, dense_fg_aux, pf_mIoU 0.8372) beat MV3-Small 8ep by ~2pp — missing |
| No E-53a closure | Direction explored and closed — should be recorded |
| No unified reeval protocol | `tools/reeval_2x2_backbone_epochs.py` provides systematic comparison — not documented |
| Training section outdated | References `tools/train.py` only; now `tools/autoresearch/run_trial_minimal.py` is primary |

---

## 7. README Update Draft

### 7.1 Replace Status section

```markdown
## Status

**Phase 2 exploration complete.** 50+ controlled trials across backbone, loss,
temporal, and auxiliary-supervision axes (E-01 → E-53a).

**Canonical baseline**: E-40 — MV3-Small + dense_fg_aux, 30ep, pf_mIoU 0.8564.
**Stronger variant**: E-51 — EfficientNet-B0 + dense_fg_aux, 8ep, pf_mIoU 0.8372
(reaches near-baseline quality in 8ep vs 30ep).

**Closed directions**: multi-scale dense supervision (E-53a, all metrics degraded).

**Active**: Teacher-route architecture exploration (E-53 archive and beyond).

See `reports/report_20260514_0800_e53a_final.md` for latest direction closure.
```

### 7.2 Add after Setup section

```markdown
## Experiments

Training uses `tools/autoresearch/run_trial_minimal.py` with JSON configs in
`configs/autoresearch/`. Outputs land in `local_runs/autoresearch/<trial_id>/`.

### Unified Reeval

```bash
python tools/reeval_2x2_backbone_epochs.py
```

Produces 2×2 (backbone × epochs) comparison at pf_mIoU level on a fixed
RandomState(42) split, matching the E-53a go/no-go evaluation protocol.
```

### 7.3 Update Training section

Replace reference to `tools/train.py` with:

```markdown
## Training

```bash
# Single trial (from config)
python tools/autoresearch/run_trial_minimal.py \
  --config configs/autoresearch/expl_NN_xxx.json

# Legacy training script (MV3-Small, current_direct_bbox)
python tools/train.py --epochs 30 --batch_size 24
```
```

---

## 8. Summary

| Area | Status |
|------|--------|
| Git health | CLEAN |
| .gitignore coverage | ADEQUATE |
| E-53a artifact completeness | COMPLETE |
| Mainline isolation | INTACT — s4 fully gated |
| EffB0 + dense_fg_aux smoke | PASSING (E-51) |
| README | **OUTDATED — update recommended per §7** |
